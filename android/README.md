# Android project

Gradle project for **Local Cache** (`app.localcache.release`).

See the [root README](../README.md) for install, config, and release notes.

```bash
./gradlew :app:assembleDebug
# APK: app/build/outputs/apk/debug/app-debug.apk
```

Default add-on port: **7100**.
